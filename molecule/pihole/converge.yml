---
# Test values live in inventories/test/group_vars/pihole/ - including the admin
# password, which comes from that group's vault via pihole_password.
- name: Converge
  hosts: pihole
  become: true

  roles:
    - role: pihole
