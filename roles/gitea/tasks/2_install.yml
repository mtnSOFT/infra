---
- name: Deploy gitea container stack
  community.docker.docker_compose_v2:
    project_src: "{{ gitea_dir }}"
    state: present
    pull: always
    remove_orphans: true
  when: gitea_deploy | bool
