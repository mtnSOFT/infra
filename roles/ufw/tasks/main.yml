---
# Task files run in order. Tags let you run a subset, e.g. `--tags reload`.
# To add a task file, drop it in this folder and add an import below.

- import_tasks: 1_ufw.yml
  tags: [ufw]

- import_tasks: 2_nat.yml
  tags: [nat, wireguard]

- import_tasks: 9_reload.yml
  tags: [reload]
